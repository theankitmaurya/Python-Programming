# Can you change the values inside a list which is contained in set S?
# s = {8, 7, 12, "Ankit", [1, 2]}

s = {8, 7, 12, "Ankit", [1, 2]}

# We cant change the value because ->
# 1. We can't add a list inside a set
# 2. We can't do indexing in set either if we can do changes in set by any way.
