# Check the type of variable assigned using input() function

a = input("Enter the value of a: ")

print(type(a))

# this gives the result as a string but f we have to get the type as a an integer or a float we have to use typecasting
